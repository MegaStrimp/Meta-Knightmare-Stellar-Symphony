///@description Main

#region Movement
bgX = (bgX + .2) % 160;
bgY = (bgY + .075) % 160;
stars1X = (stars1X + .16) % 160;
stars1Y = (stars1Y + .06) % 160;
stars2X = (stars2X + .12) % 160;
stars2Y = (stars2Y + .04) % 160;
#endregion