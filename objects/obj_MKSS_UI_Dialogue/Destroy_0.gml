///@description Destroy

#region Revert Variables
if (disableHud) global.hasHud = true;
if (disableGamePause) global.canGamePause = true;
if (disableMovement) global.MKSS_CutsceneStopMovement = false;
#endregion